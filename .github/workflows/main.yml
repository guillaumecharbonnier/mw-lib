name: Tests

on: [pull_request, push]

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
    - uses: snakemake/snakemake-github-action@v1.13.0
      with:
        directory: '.'
        args: '--cores 1 --use-conda --conda-cleanup-pkgs cache tests_light'
