name:
  mysql
channels:
  - ostrokach
  - conda-forge
  - defaults
dependencies:
  - mysql-client
#- mysql #mysql: error while loading shared libraries: libtinfo.so.6: cannot open shared object file: No such file or directory         

