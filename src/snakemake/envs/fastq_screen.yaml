name:
  fastq_screen
channels:
  - conda-forge
  - bioconda
  - nodefaults
dependencies:
  - fastq-screen
  - perl-gdgraph # required to generate png output and avoid this error
                 # Perl module GD::Graph::bars not installed, skipping charts
